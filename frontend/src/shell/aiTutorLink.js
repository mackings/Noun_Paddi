// Theresa (AI Tutor) lives at asktheresa.com, not as an in-app page — see App.js's
// ExternalRedirect for the same URL used elsewhere in the app.
export const AI_TUTOR_URL = 'https://asktheresa.com/';
